<?xml version="1.0" standalone="yes"?>
<rss xmlns:sparkle="http://www.andymatuschak.org/xml-namespaces/sparkle" version="2.0">
  <channel>
    <title>Orchard</title>
    <item>
      <title>Version 2.4.1</title>
      <sparkle:version>59</sparkle:version>
      <sparkle:shortVersionString>2.4.1</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>26.0</sparkle:minimumSystemVersion>
      <link>https://github.com/andrew-waters/orchard/releases/tag/v2.4.1</link>
      <description><![CDATA[
<h3>Added</h3>
<ul>
<li>The k8s Clusters tab now warns when the guest kernel cannot bootstrap a cluster, and offers to fix it. <code>container k8s create</code> prepares its node with <code>iptables-nft</code>, which needs a kernel built with nftables; the kernel Apple container recommended before 1.3.0 was not, and <code>container system start</code> skips the kernel download whenever a kernel already exists (apple/container#905), so an install carried forward from an earlier release keeps the old kernel and fails every cluster creation however new the CLI is. Orchard reads the default kernel the CLI records, and when it predates nftables the tab says so and offers &quot;Install Recommended Kernel&quot; in place of the empty state. Creation is still available from there, because the check is a version floor rather than a capability probe and a deliberately chosen custom kernel is nobody&#x27;s business but yours.</li>
</ul>
<h3>Changed</h3>
<ul>
<li>Sheets have one Cancel button rather than two. Create Cluster, Create Machine, Edit Machine, Add Network and Add DNS Domain each had one in the header and another in the footer, and several bound the Escape key twice over. The footer keeps its Cancel, beside the action it cancels.</li>
<li>The log viewer&#x27;s target picker now shares a row with the filter field instead of sitting on a row of its own above a divider, which gives every pane back a band of vertical space; a window split four ways spent four of them on it. The picker also sits flush with the pane&#x27;s leading edge: a menu picker centres itself in a flexible frame, which was insetting it from everything above and below.</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Opening Containers or Images no longer highlights a row from the middle of the list. Both lists sort and filter in the view (by name, running first, the search field, the in-use toggle), while the selection was made from the first element of the service&#x27;s array, which is the order the daemon returned. On this machine that meant Images opened on a digest-pinned Kubernetes node image nine rows down. Each of those lists now claims the first row it is actually showing.</li>
<li>A single-node Kubernetes cluster is no longer treated as though it had no control plane. Container 1.4.1 labels the one node of a default <code>container k8s create</code> cluster <code>control-plane,worker</code>, because it is both, and Orchard compared that label to <code>control-plane</code> exactly. The node therefore classified as a worker, the cluster read as stopped while it was running, and everything gated on a running control plane was hidden: the detail header offered Start instead of Terminal (kubectl), Write Kubeconfig, Copy Kubeconfig Path and Load Image, and every resource panel showed &quot;Not running&quot; over live charts. Roles are now read as the list the label can be, so a node that is both counts as the control plane.</li>
<li>A cluster that fails while its node is being prepared now explains why. The plugin runs node preparation as one <code>set -e</code> script whose last two commands are the <code>iptables-nft</code> calls, so an abort there surfaces the accumulated output of the steps that already succeeded: the error names <code>net.ipv4.ip_forward = 1</code>, a sysctl that worked, and never shows the command that failed (apple/container#2120, open with no fix in 1.4.1). Orchard recognises that failure and names the real cause, including the kernel responsible when it can tell.</li>
<li>&quot;Install Recommended Kernel&quot; no longer reports success without installing anything. An &quot;already exists&quot; refusal from the CLI was treated as an idempotent success, which is wrong in the one case that matters most: someone on an outdated kernel would be told the recommended one was installed and would still fail every cluster creation. The refusal is now retried with <code>--force</code>, which replaces only a kernel of the same name.</li>
</ul>
]]></description>
      <pubDate>Tue, 15 Sep 2026 13:24:17 +0000</pubDate>
      <enclosure url="https://github.com/andrew-waters/orchard/releases/download/v2.4.1/Orchard-2.4.1.dmg" type="application/octet-stream" sparkle:edSignature="+nlFPX6FT37CpAh6kwRVrgdaDy3OXblNe9WH63MxqU1UqQEUTFgI2/2aiCT1V1qVPI1WC9w3+paIsfObgCutBA==" length="29747034" />
    </item>
  </channel>
</rss>
