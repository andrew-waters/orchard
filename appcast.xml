<?xml version="1.0" standalone="yes"?>
<rss xmlns:sparkle="http://www.andymatuschak.org/xml-namespaces/sparkle" version="2.0">
  <channel>
    <title>Orchard</title>
    <item>
      <title>Version 2.2.2</title>
      <sparkle:version>51</sparkle:version>
      <sparkle:shortVersionString>2.2.2</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>26.0</sparkle:minimumSystemVersion>
      <link>https://github.com/andrew-waters/orchard/releases/tag/v2.2.2</link>
      <description><![CDATA[
<h3>Added</h3>
<ul>
<li>The menu bar popup now exposes a Stop control for the container system alongside the existing Start control, so the system can be shut down without opening the main window or going through the command palette. The button is gated on a running system and disabled while a transition is in flight; calling <code>stopSystem()</code> while a transition is already in flight is a no-op at the service layer.</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Menu-bar System analytics no longer stays stuck on &quot;Collecting…&quot; when container stats fail. The menu-bar hover popover now distinguishes four explicit states: collecting during the initial two-tick baseline, available with real CPU/memory history, &quot;No running containers&quot; when the system has none to sample, and &quot;Stats unavailable&quot; with a short reason when the XPC service is unreachable or every stats call failed. Per-container stats failures are now logged via <code>Log.xpc</code> so diagnostics aren&#x27;t blind, and the existing dashboard, container, and machine stats paths are unchanged. No new macOS permission is required.</li>
<li>Container service-unavailable failures in the menu-bar sampling tick are now logged only once per tick via <code>Log.xpc</code>; subsequent outcomes in the same tick fold in silently while the first error is retained, so a single tick with several unreachable containers no longer produces one log line per outcome.</li>
</ul>
]]></description>
      <pubDate>Tue, 25 Aug 2026 08:27:25 +0000</pubDate>
      <enclosure url="https://github.com/andrew-waters/orchard/releases/download/v2.2.2/Orchard-2.2.2.dmg" type="application/octet-stream" sparkle:edSignature="MMkimdcBh9vsW6+ZS6OScBlIsgxhuxQ09w/gvD1iskYq3Rz1UkRxFsWxVj/5zFL21unKcx5xVy96f5Sr/qcRCQ==" length="29340534" />
    </item>
  </channel>
</rss>
