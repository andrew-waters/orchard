<?xml version="1.0" standalone="yes"?>
<rss xmlns:sparkle="http://www.andymatuschak.org/xml-namespaces/sparkle" version="2.0">
  <channel>
    <title>Orchard</title>
    <item>
      <title>Version 1.12.6</title>
      <sparkle:version>38</sparkle:version>
      <sparkle:shortVersionString>1.12.6</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>26.0</sparkle:minimumSystemVersion>
      <link>https://github.com/andrew-waters/orchard/releases/tag/v1.12.6</link>
      <description><![CDATA[
<h3>Added</h3>
<ul>
<li>Live resource charts for every container — CPU, memory, network, and disk over time — on the container Overview, plus a system-wide dashboard in the Dashboard view that sums usage across all containers. Charts have selectable time windows (5m / 15m / 1h / 24h) and hover tooltips, and the container list gains a per-row CPU sparkline.</li>
<li>Real CPU usage percentage (previously a placeholder that always read 0%). Stats are sampled continuously in the background and the history is saved between launches, so the longer time windows have data to show. Sampling pauses while the app is hidden to save resources.</li>
<li>A redesigned menu-bar panel: CPU and memory usage rings across all running containers, a per-container list with start/stop controls, and hover-to-reveal history panels (per container, and system-wide) showing the last hour of CPU and memory.</li>
</ul>
<h3>Changed</h3>
<ul>
<li>Redesigned the container detail view into a single scrolling page (no more Overview/Environment/Mounts/Logs tabs): the resource metrics show as CPU, Memory, Network, and Disk panels pairing current values with their graph (network/disk graphs plot inbound above the axis and outbound below), and the remaining configuration sits in boxed sections below. Environment values are hidden until you click Show, and the image reference now appears under the container name in the header. Logs and Edit Configuration moved into the header, and the Logs button opens on the container you&#x27;re viewing.</li>
<li>Reworked the Stats tab into a <strong>Dashboard</strong> — now the default view when the app opens — with disk-usage headline tiles, per-metric panels (CPU / Memory / Network / Disk) each pairing current values with a graph, and per-metric sparklines in the container table.</li>
<li>Copy controls across the app now read &quot;Copy&quot; and confirm with &quot;Copied&quot; instead of an icon.</li>
<li>Ongoing performance and maintainability improvements to the app&#x27;s internals — views now refresh only when the data they display actually changes.</li>
<li>Refactored the internals: the monolithic container service was split into focused per-domain services with each view observing only what it needs, the Run and Edit container forms now share one implementation, and a UI smoke-test harness was added. No user-facing behaviour change.</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>Network subnet validation now rejects out-of-range addresses (e.g. <code>999.999.999.999/24</code>); each octet must be 0–255.</li>
</ul>
]]></description>
      <pubDate>Sun, 05 Jul 2026 15:09:56 +0000</pubDate>
      <enclosure url="https://github.com/andrew-waters/orchard/releases/download/v1.12.6/Orchard-1.12.6.dmg" type="application/octet-stream" sparkle:edSignature="LMnaeUnZZ0FurmYTCQoK4oaYq8m1wvvZs1Lxh09pnfdmQB0gx4Tidg6TLNO5oBRdlzuFG4VN+t4h+hdHU3dxBg==" length="27197662" />
    </item>
  </channel>
</rss>
